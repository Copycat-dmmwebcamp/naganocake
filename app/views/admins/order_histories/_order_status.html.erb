<%= form_with(model: [:admins, order], url: admins_order_history_path) do |f| %>
<%= f.select :order_status, Order.order_statuses.keys.map {|k| [I18n.t("enums.order.order_status.#{k}"), k]} %>
　<%= f.submit "更新", class: "btn btn-primary" %>
<% end %>